{
  "name": "alegieri",
  "version": "3.0.0",
  "private": true,
  "author": "ilunya100000",
  "description": "Алегьери — учебное PWA-приложение для 9 В",
  "scripts": {
    "start": "node server.js",
    "check": "node tests.mjs",
    "check:release": "node --check server.js && node --check database.js && node --check public/app.js && node tests.mjs",
    "setup-admin": "node setup-admin.js",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "android:build": "npx cap build android"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "engines": {
    "node": ">=22"
  }
}
